# Dev Session Log — 2026-06-13

## 1. Apa yang dibangun hari ini

- **Hermes update v0.16.0** — pull 109 commits, build web UI + desktop app, sync skills
- **Research workspace setup** — restructure `docs/abelion/`:
  - `research/{hermes-agent,ai-architecture,security}/`
  - `notes/`, `scratch/`
  - `.gitignore` untuk binary files (mp3, csv, m4a)
  - `README.md` index
  - Push ke branch `research`
- **Git remote fix** — switch origin dari HTTPS ke SSH (`git@github.com:Abelion512/hermes-agent.git`)
- **Git identity setup** — `user.name="Abelion512"`, `user.email="abelion512@users.noreply.github.com"`
- **Nested .git cleanup** — hapus `.git` di `plugins/hermes-plugin-agent-orchestrator/` yang bikin `hermes update` ignore path itu
- **Branch `research`** — created dari `jules`, dedicated untuk research workspace
- **PR audit** — identify 2 potential PRs (write-text-encoding, sql-safe-queries), tapi di-kill karena low-value/cosmetic
- **Issue #37011 investigation** — file descriptor leak di platform reconnect loop. Ternyata sudah di-fixed di v0.16.0 (semua 3 failure paths sudah panggil `_dispose_unused_adapter()`)

## 2. Masalah yang muncul

- **`docs/abelion/` path hilang** — ternyata HEAD detached di `upstream/main`, gak ada `docs/abelion/` di sana. Fix: checkout ke branch `jules` → ✅ solved
- **Git auth gagal** — HTTPS remote gak ada credential di host terminal. Fix: switch ke SSH → ✅ solved
- **`npm audit fix` gagal** — ENOLOCK error di root, workspace graph corruption (`Cannot read properties of null`). Belum fix → ⚠️ open (optional, low priority)
- **Baileys critical CVE** — `baileys@7.0.0-rc.x` punya message spoofing vuln, no fix available → ⚠️ open (unrelated, WhatsApp bridge)
- **2 PR branches di-delete** — `pr/write-text-encoding` dan `pr/sql-safe-queries` dihapus karena cosmetic → ✅ intentional

## 3. Yang gw pelajarin

- **Hermes codebase udah mature** — v0.16.0 security patterns solid, error handling mostly proper, gak banyak low-hanging fruit. Issue yang di-report di GitHub udah banyak di-fix.
- **Research ≠ cuma baca** — research yang impactful itu gap → experiment → synthesize → share. Bukan cuma kutip paper.
- **Anthropic research model** — hire for mission alignment first, capability second. Research → product pipeline, gak ada research murni "untuk research".
- **Indonesia non-blok = competitive advantage** — bisa access semua sumber daya (AS, China, EU) tanpa locked ke satu geopolitical narrative. Relevant buat S2 application.
- **Gap > paper** — mau masuk Anthropic research, harus demonstrate real contribution (PR, public track record, publishable work)

## 4. Fokus besok

1. **Define research topic** — pick satu focus area dari intersection (RSI / multi-agent / security) untuk di-develop jadi proper research plan
2. **S2 prep** — research Tsinghua vs Zhejiang vs HITSZ: admission requirements, research groups, faculty yang align sama interest loe
3. **Hermes contribution (optional)** — kalau sempat, cek issue #44588 (OAuth shared globals) atau #5059 (surrogate UTF-8 crash) untuk PR yang lebih substantial
